# Secrets — never commit
.env
*.env
!*.env.example
creds.env
*.key
*.pem
!config/examples/*.example*

# Local data and backups
data/
ops/backups/
ops/reports/*.tmp
reporting/output/
*.tar.gz
*.sql.gz
*.zip
*.pcap
*.evtx

# Generated per-client artifacts (contain live private keys)
scripts/endpoint-deploy/client.config.yaml
ops/reports/shuffle-periodic-repair.log

# Editor and OS noise
.DS_Store
*.swp
*~

# Operational logs
ops/reports/*.log
ops/reports/vm103-*.log

# White-label real values (never commit)
config/brand.yml
config/clients/
__pycache__/
