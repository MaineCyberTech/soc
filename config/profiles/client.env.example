# MCT Security Stack - CLIENT profile (placeholders only - no secrets)
CLIENT_ID=${CLIENT_ID}
CLIENT_NAME=${CLIENT_NAME}
CLIENT_ENDPOINT_COUNT=${CLIENT_ENDPOINT_COUNT}
SCAN_SCOPE=${SCAN_SCOPE}
SCAN_AUTH_FILE=${SCAN_AUTH_FILE}
