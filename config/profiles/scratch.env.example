# MCT Security Stack - SCRATCH (DR drill) profile (placeholders only - no secrets)
SCRATCH_ADMIN_PASSWORD=${SCRATCH_ADMIN_PASSWORD}
SCRATCH_REPO_LOCATION=${SCRATCH_REPO_LOCATION}
SCRATCH_PORT_HTTP=${SCRATCH_PORT_HTTP}
SCRATCH_PORT_TRANSPORT=${SCRATCH_PORT_TRANSPORT}
SCRATCH_INDEX_PREFIX=${SCRATCH_INDEX_PREFIX}
