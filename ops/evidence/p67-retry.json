{
  "description": "Phase 67 retry/dead-letter/replay DESIGN — now PARTIALLY IMPLEMENTED in the Class-A workflow execute_python (OpenSearch workflow-000001 doc c6b3fcd8, updated 2026-08-28, backup at ops/backups/workflow-c6b3fcd8-20260828T223000Z.json). Retry loop + dead-letter are wired; idempotency/replay/alerting remain deferred (noted).",
  "max_attempts": "3 — IMPLEMENTED in workflow execute_python (retry loop, breaks on first success)",
  "timeout_seconds": "15 — IMPLEMENTED (per-request timeout in requests.post)",
  "backoff": "exponential (2s,4s,8s) — IMPLEMENTED (time.sleep(backoff**attempt))",
  "idempotency_key": "NOT enforced in code (IRIS list API 500s); best-effort via alert tags source:wazuh,class:A; deferred",
  "dead_letter": "IMPLEMENTED — on final failure execute_python prints state=DEAD_LETTER (Shuffle execution result is the dead-letter record; Shuffle can alert on failed executions)",
  "replay_guard": "NOT implemented — IRIS list API 500s prevents pre-check; deferred (replay could duplicate; tags aid identification)",
  "alerting": "NOT implemented — no Shuffle notification wired on dead-letter; deferred"
}
