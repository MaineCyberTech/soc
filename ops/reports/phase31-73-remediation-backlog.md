# Phase 31 Remediation Backlog (P0-P3)

Date: 2026-08-24

## P0
1. SPAN mirror approval -> production Suricata deploy + full-volume benchmark (packet gap).
2. Endpoint markers 013/014 (operator RMM) -> cert -> throttles.

## P1
3. RAM expansion (core host) for durable headroom.
4. Agent-disconnect + watermark alert wiring into a live scheduler (36-40).
5. Disk: confirm 08-15..18 wave (~08-29).

## P2
6. Shuffle UI native controls (approval).
7. Fresh target provision -> deployability PASS + full-cluster drill.
8. Credential/owner closure (VT, PVE, indexer, NetFlow, Redis, Greenbone, canarytokens).

## P3
9. NetFlow alert arming (scope); SLO formalization; sensor ruleset expansion (with care).

## No secrets
