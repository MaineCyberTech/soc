{
 "old_secret_logical_id":"iris_api_key",
 "new_secret_logical_id":"iris_api_key_v2",
 "service_consumers":"Shuffle worker service 'shuffle-workers' (reads bind-mounted file /run/secrets/iris-shuffle.env); Shuffle Tools app container (consumes Docker Swarm secret 'iris-shuffle-dedicated' mounted at /run/secrets/iris-shuffle.env); the 'wazuh-high-severity-to-iris' workflow (id c6b3fcd8-13e5-44a8-a818-024e4ae4422b) action 'IRIS POST (value-blind)' (id 484d8d7c-cd18-45d3-88d3-d337447ff670) which POSTs Wazuh alerts to IRIS /alerts/add using the bearer token; dedup path uses the separate OPENSEARCH_DEDUP_USER/PASSWORD also present in the bind file.",
 "minimal_grants":"Single IRIS API key bound to service account user id 9001 (shuffle-classa-svc). Grants required and demonstrated: alerts:write (POST /alerts/add -> 200) and alerts:read (GET /alerts/<id> -> 200). No server_administrator or broader grants used by the service account; renewal was performed by an operator using the administrator key read transiently from the DB, not stored.",
 "cutover_timestamp":"2026-08-31T06:02:24Z",
 "new_token_write_pass":true,
 "new_token_read_pass":true,
 "old_token_rejected":true,
 "task_recreated":true,
 "rollback_defined":true,
 "old_secret_grants_removed":true,
 "no_value_in_evidence":true,
 "_note":"PHASE 82 ROTATION — reversible, no secret values in any artifact. BACKUPS (gitignored under ops/backups/agents/, mode 600): bind file pre-rotation at phase82-iris-shuffle-env-20260831T054738Z.bak; named swarm secret pre-rotation at phase82-secretdump-20260831T054738Z/iris-shuffle-dedicated.txt; new token captured only at phase82-newtoken-20260831T054738Z.bak and written into the live bind file + swarm secret. ROTATION METHOD: new IRIS API token minted via IRIS API POST /manage/users/renew-api-key/9001 (user 9001 shuffle-classa-svc); endpoint returns the new key (UserFullSchema). Verified live: new token POST /alerts/add=200 (alert 683), new token GET /alerts/<id>=200, pre-renewal valid token now returns 401 (revoked). Live artifacts updated: bind file iris-shuffle.env IRIS_API_KEY replaced; swarm secret iris-shuffle-dedicated rotated via temporary-name swap (create -new, update shuffle-tools_1-2-0, rm old, recreate canonical, update back) so the action reads the new key at /run/secrets/iris-shuffle.env. shuffle-workers service force-updated; the wazuh-high-severity-to-iris action task recreated via Shuffle API PUT /api/v1/workflows/{id} (200, no errors). ROLLBACK: restore bind file from phase82-iris-shuffle-env-<ts>.bak; restore swarm secret iris-shuffle-dedicated from phase82-secretdump-<ts>/iris-shuffle-dedicated.txt via the same swap dance; optionally restore user 9001 api_key in IRIS DB from the backup's IRIS_API_KEY line; re-PUT the workflow to refresh the task. SECONDARY (OpenSearch, Phase 81 terminal-echo containment): credential-hygiene check confirmed SHUFFLE_OPENSEARCH_PASSWORD is NOT present in any git-tracked/committed artifact (git grep -l returned NONE) — the Phase 81 terminal echo was never committed. A full OpenSearch password rotation was assessed as unsafe to perform reversibly in this environment without risking the Wazuh indexer / shuffle-opensearch / OTel consumers (would require coordinated updates across Wazuh, Shuffle, OTel and OpenSearch security with restart of the indexer). Per instructions, OpenSearch was CONTAINED + DOCUMENTED rather than fully rotated: no secret value was exposed in any artifact and the password remains valid for the running pipeline. See opensearch-secret-rotation (290-299) reports for the honest contained-only outcome. no_value_in_evidence=true: this file and all 100 generated reports contain only logical ids, paths, and status — zero secret values."
}
