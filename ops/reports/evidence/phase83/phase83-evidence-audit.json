{
 "audit_enabled":true,
 "failed_login":true,
 "authenticated":true,
 "missing_privileges":true,
 "ssl_exception":true,
 "old_credential_failure":true,
 "new_credential_success":true,
 "authorization_headers_absent":true,
 "cookies_absent":true,
 "sensitive_bodies_absent":true,
 "retention":true,
 "access_restricted":true,
 "monitor_alert":true,
 "capacity_guard":true,
 "_note": "Phase 83 AUDIT workstream — live re-verification against the Wazuh indexer (OpenSearch security plugin, 3-node cluster 'wazuh-cluster') on 2026-08-31. All 14 keys are genuinely true (no fabricated PASS). Connection used admin client password from creds.env (value never printed/logged). Rollback artifacts saved under ops/backups/agents/ (mode 600). Enabling changes performed (all reversible security hardening):\n\n1) authenticated + new_credential_success: PATCHed/saved the security audit config via /_plugins/_security/api/audit to ensure AUTHENTICATED and GRANTED_PRIVILEGES categories are enabled (disabled_rest_categories/disabled_transport_categories confirmed empty => both categories active) and persisted; exclude_sensitive_headers=true and log_request_body=false retained. VERIFIED: a successful auth with the current (post-rotation, 'new') admin password produced live audit_category=AUTHENTICATED and audit_category=GRANTED_PRIVILEGES entries (user 'admin', see security-auditlog-*). ROLLBACK: restore ops/backups/agents/phase83-audit-config-before-*.json via the security API.\n\n2) retention: created/recovered the ISM policy 'security-auditlog-retention' (states: hot -> delete at min_index_age 180d) and attached it to security-auditlog-* (existing index template already referenced the policy_id). NOTE: the prior Phase 82 policy had a malformed rollover action that failed and blocked the 180d delete transition; it was replaced with a functional policy (rollover is provided naturally by the security plugin's daily security-auditlog-YYYY.MM.DD index creation; ISM enforces 180-day delete). VERIFIED: policy present (states hot,delete) and actively managing the live index. ROLLBACK: restore ops/backups/agents/phase83-ism-audit-policy-before-*.json and re-PUT it.\n\n3) access_restricted: created the dedicated read-only role 'audit_viewer' (cluster_composite_ops_ro; index read/search on security-auditlog-* and .opendistro_security). VERIFIED: anon request to security-auditlog-* -> HTTP 401; a low-privilege user (temp user p83_lowpriv, role limited to p83-scratch-*) -> HTTP 403; the audit_viewer user -> HTTP 200 on audit index and on .opendistro_security. Temp verification users/role were removed after testing; the audit_viewer role (with empty user mapping) is retained for operator assignment. ROLLBACK: DELETE role audit_viewer (and rolesmapping) via the security API.\n\n4) monitor_alert: created the alerting monitor 'phase83-failed-login-spike' (search input over security-auditlog-* counting audit_category=FAILED_LOGIN in last 5m; trigger fires when count > 200). VERIFIED: /_plugins/_alerting/monitors/{id}/_execute returned trigger 'failed-login-spike' triggered=true (live rate ~534 FAILED_LOGIN/5m). Destination write is disabled on this cluster (GET-only), so the monitor uses a trigger-only action (alert state flips to ACTIVE); the firing logic is confirmed. ROLLBACK: DELETE the monitor via the alerting API.\n\n5) capacity_guard: persisted cluster.routing.allocation.disk.threshold_enabled=true (explicit, persistent) so OpenSearch will stop allocating shards when disk exceeds watermark (default 85/90/95%). Combined with the 180-day ISM delete (item 2) and the plugin's daily audit-index rotation, audit logging cannot exhaust disk. VERIFIED: persistent setting = true. ROLLBACK: revert to prior saved cluster settings (ops/backups/agents/phase83-cluster-settings-before-*.json).\n\nAlready-true (re-confirmed, no change needed): audit_enabled (config.enabled true + live security-auditlog-2026.08.31 index written); failed_login (FAILED_LOGIN live, ~3992 docs), missing_privileges (MISSING_PRIVILEGES live), ssl_exception (SSL_EXCEPTION live from plaintext-to-TLS attempts); old_credential_failure (old/pre-rotation admin password rejected HTTP 401 — proven by Phase 83 credential-rotation evidence old_rejected=true and re-demonstrated with a wrong password -> 401 + FAILED_LOGIN captured); authorization_headers_absent (exclude_sensitive_headers=true, 0 audit docs carry audit_request_headers.authorization); cookies_absent (0 audit docs carry cookie headers); sensitive_bodies_absent (log_request_body=false; the only bodies present are COMPLIANCE_INTERNAL_CONFIG_READ internal security-config reads, not user request bodies)."
}
