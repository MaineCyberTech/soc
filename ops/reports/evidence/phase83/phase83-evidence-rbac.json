{
  "identity": "soc_least_priv",
  "required_operations": "read/search/get (indices:data/read/*) on operational SOC indexes only; read-only cluster monitoring (cluster:monitor/health, cluster:monitor/state, cluster:monitor/nodes/info, cluster:monitor/nodes/stats, cluster:monitor/main). No write, no bulk write, no cluster admin, no wildcard index pattern.",
  "representative_requests": "GET/POST <index>/_search on security-auditlog-* and workflowexecution-*; GET <index>/_count; GET _cluster/health; GET _mget on workflowexecution-*; GET _cat/indices (monitor).",
  "permission_checks": "Executed as soc_least_priv (temp user soc_test_lp, removed post-verification): (1) GET security-auditlog-*/_search -> 200 OK (positive control); (2) GET _cluster/health -> 200 OK (monitor perm granted); (3) GET users/_search (index outside required_indexes) -> 403 Forbidden (unrelated_indexes_denied); (4) PUT _plugins/_security/api/roles/soc_should_fail (cluster-admin security API) -> 403 Forbidden (cluster_admin_denied); (5) GET .opendistro_security/_search (security index) -> 403 Forbidden (security_index_denied).",
  "required_indexes": "security-auditlog-*, workflowexecution-*, workflow-*, workflowapp-*, shuffle_logs-*, wazuh-iris-dedup-*, ss4o_traces-otel-mct-soc, top_queries-*",
  "unrelated_indexes_denied": true,
  "cluster_admin_denied": true,
  "security_index_denied": true,
  "readall_mappings inventoried": true,
  "wildcard_reduced_or_exception": true,
  "exception_expiry_or_na": "2026-09-30",
  "_note": "READALL INVENTORY: The 'readall' role is reserved/static (PUT returns 403, cannot scope its index_patterns away from '*' at the role level). It grants index_patterns ['*'] with allowed_actions ['read'] plus cluster_permissions ['cluster_composite_ops_ro']. Its rolesmapping was backend_roles:['readall'], which granted the wildcard to every principal carrying backend_role 'readall' - namely internal users 'kibanaro' (also kibanauser) and 'readall'. Full before-state backed up to ops/backups/agents/phase83-readall-role-before-20260831T080500Z.json and ops/backups/agents/phase83-readall-rolesmapping-before-20260831T080500Z.json (plus full rolesmapping and both internal-user records). readall_mappings inventoried=true. REDUCTION / EXCEPTION: Because the role is static, the wildcard was reduced at the mapping layer - the backend_role catch-all was removed so 'kibanaro' no longer inherits the readall '*' grant; only the explicit service user 'readall' retains it. The residual readall grant is governed by a time-bound EXCEPTION expiring 2026-09-30. wildcard_reduced_or_exception=true; exception_expiry_or_na=2026-09-30. NEW LEAST-PRIVILEGE IDENTITY: Role 'soc_least_priv' created (PUT 201) with explicit index_patterns (no '*') and minimal allowed_actions (read, indices:data/read/mget, indices:data/read/get) plus read-only cluster monitor perms. A temporary user 'soc_test_lp' was mapped to it, used to verify positive + negative authorization, then deleted (GET now 404); the soc_least_priv role persists as the established identity. ROLLBACK: Restore the readall mapping from ops/backups/agents/phase83-readall-rolesmapping-before-20260831T080500Z.json via PUT _plugins/_security/api/rolesmapping/readall. Optional: DELETE role soc_least_priv if the least-privilege identity is no longer wanted. No secret values are stored in this evidence or in any report."
}