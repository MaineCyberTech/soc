{
 "p82_incident_status":"closed",
 "branch_dispositions":{"iris_api_key":"rotated_revoked","opensearch_admin_password":"rotated_revoked"},
 "phase83_echo_observation_id":"P83-ECHO-OBS-001",
 "echo_was_revoked_material":true,
 "no_active_secret_exposed":true,
 "credential_catalog":"Reviewed credential catalog (value-blind): (1) iris_api_key — rotated in Phase 82; old IRIS API token revoked (returns 401), new token active for service account 9001 (shuffle-classa-svc); grants limited to alerts:write + alerts:read. (2) opensearch_admin_password (Wazuh indexer/OpenSearch 'admin' user) — rotated in Phase 83; old password rejected (401), new password active (200), admin grants preserved. (3) shuffle-opensearch reserved admin — a reserved/static OpenSearch Security user, intentionally NOT rotated (REST edits of reserved users are forbidden and no admin client-certificate path exists); governed as reserved. (4) dedicated Docker Swarm secrets iris-shuffle-dedicated, dedup-shuffle-dedicated, iris-ca.crt, opensearch-ca — service-scoped and current. No active exposed credential remains in the catalog.",
 "consumer_inventory":"iris_api_key consumers: Shuffle worker service 'shuffle-workers' (bind file /run/secrets/iris-shuffle.env); Shuffle Tools app container (swarm secret iris-shuffle-dedicated); wazuh-high-severity-to-iris workflow action 'IRIS POST (value-blind)' (POST /alerts/add as bearer token); dedup path uses separate OPENSEARCH_DEDUP_* credentials (not the iris key). opensearch_admin_password consumers: filebeat inside wazuh-manager (output.elasticsearch.username/password on master + worker nodes) and admin-API/CLI operations. Documented non-consumers: wazuh-manager connects to the indexer via mutual TLS client certificate (not the admin password); wazuh-dashboard uses the 'kibanaserver' user. shuffle-opensearch reserved admin: no runtime consumer in this deployment (reserved, not in active service binding).",
 "secret_grants_current":true,
 "reserved_admin_disposition":"The reserved shuffle-opensearch admin (a reserved/static OpenSearch Security user) was reviewed and governed WITHOUT a false rotation claim: OpenSearch Security forbids REST edits of reserved users and no admin client-certificate path is available for it, so it was intentionally NOT rotated in Phase 82/83. It is tracked as reserved with documented rationale; only the operational 'admin' user password was rotated (Phase 83). No false 'rotated' status is asserted for the reserved user.",
 "rotation_readiness":"Rotation readiness is established for future rotations: both Phase 82 (iris_api_key) and Phase 83 (opensearch_admin_password) rotations captured timestamped, mode-600, gitignored backups (bind-file + named swarm-secret dumps; internal_users.yml/filebeat/ossec/dashboard snapshots) and documented rollback plans (phase83-wazuh-rollback-plan-20260831T073100Z.md and the Phase 82 token-restore procedure). Versioned secrets (iris_api_key_v2, opensearch_admin_password_v2) with stable target paths were used. Future rotations inherit this backup+rollback posture.",
 "no_values_or_fingerprints_in_evidence":true,
 "_note":"RECONCILIATION (value-blind). Phase 82 (P82-CRED-EXP-001): terminal credential exposure incident from Phase 81, CLOSED after the iris_api_key was rotated+revoked in Phase 82 (phase82-evidence-rotation.json: old token 401, new token 200, reversible via backups) and the opensearch_admin_password was rotated+revoked in Phase 83 (phase83-evidence-rotation.json: old password 401, new password 200, filebeat converged, grants preserved). phase83-evidence-exposure.json re-affirms both branches as rotated_revoked with incident_status=closed, determined by value-blind scanning (by variable name/pattern only; no secret VALUE printed, echoed, hashed, or committed across shell history, session logs, process args, artifacts, git history, backups). Phase 83 terminal-echo observation (P83-ECHO-OBS-001): during the Phase 83 RBAC step a fragment of the OLD indexer admin password was echoed to a terminal. That material was the ALREADY-REVOKED old password (rotation had already occurred; old value returns 401 and is non-live), so the echo_was_revoked_material=true and no_active_secret_exposed=true. No live credential was exposed; the echoed fragment was not committed. All Phase 84 evidence contains zero secret values and zero secret fingerprints."
}
