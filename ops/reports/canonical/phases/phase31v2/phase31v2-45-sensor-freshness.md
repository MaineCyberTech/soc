# Phase 31v2 Packet Sensor Freshness

Date: 2026-08-24
- Monitor: eve.json mtime (p31v2-source-freshness.sh), mct-suricata state + MemoryCurrent,
  SPAN rx counters, drops (stats), Wazuh ingest (agent 016). Wire to scheduler (Phase 32).

## No secrets
