# Round 3 Complete — Crons, MISP CDB, Class B, OpenCanary Monitor (2026-08-10)

## Crons installed (root crontab)

| Cron | Schedule | Purpose |
|---|---|---|
| backup-phase2-config.sh | 04:00 daily | Phase 2 config archive |
| phase2-healthcheck.sh | Mon 06:30 | Weekly healthcheck |
| misp-to-wazuh-cdb.py --push | 03:15 daily | MISP IOC → Wazuh CDB |

## MISP → Wazuh CDB — DEPLOYED & VERIFIED

- Production script (stdlib-only), list `etc/lists/malicious-ioc/misp-iocs` in both manager confs, 5 rules (121100-121104, level 12)
- Verified: test IOCs → CDB → rule 121100 fired (both IPs); auto-reload without restart; cleanup worked
- MISP reachability: MISP now binds the VM LAN IP (192.168.222.154:8443) with an iptables allowlist (192.168.222.0/24 + lo; INPUT DROP default; rules persisted to /etc/iptables/rules.v4). Gotcha: FORWARD must stay ACCEPT (docker DNAT path) — initial DROP broke all container traffic.

## Class B workflow (wazuh-flow-classb-to-iris)

- Severity 5 (High), class:B tag; hook B recreated pointing at it (no hook-update API in this Shuffle build — recreate with same UUID)
- Verified: webhook → alert #7 (High), then deleted

## OpenCanary → IRIS monitor

- Monitor `opencanary-hit` (1-min): wazuh-alerts-4.x-*, rule.groups=opencanary, level 12, last 2 min → Class A webhook
- Verified: canary hit → IRIS alert #6 in 5s (deleted after)

## Incidents handled

- local_rules.xml corrupted twice by a bad python rewrite (duplicated blocks) → rebuilt from timestamped backup + opencanary + misp rules; final file validated (84/84 groups balanced, 8508 rules loaded)
- Shuffle worker/app containers needed `docker network connect mct-security` (worker→backend DNS, http app replicas→iriswebapp_nginx)
- IRIS alert_tags must be comma-string; HTTP app parameter names verified against the installed app schema

## Remaining (operator or future)

- Alert → case escalation: requires Shuffle execution-variable chaining (likely broken like ${body:} in this build) — manual escalation for now
- Shuffle webhook variable enrichment (${body:...} unresolved in this version)
- Credential rotation, Sysmon pilot, Greenbone credentialed scans
- Re-run `docker network connect mct-security` for new Shuffle workers/app replicas after restarts

## Round 3b (22:40 UTC) — Greenbone alert + reporting crons

### Greenbone critical-finding alert → Shuffle → IRIS — LIVE
- Alert `MCT-Critical-to-Shuffle` (id 0daca165-bb08-472c-a59a-4fa1e84bd5e7): condition Severity at least 9.0, event Task run status changed, method HTTP Get → Shuffle webhook A
- **GMP gotcha**: the name elements (condition/event/method) must have NO trailing space before `<data>` children — `"Severity at least "` fails with "Failed to recognise condition name"; `"Severity at least<data>..."` works. Also event data child optional; method name is "HTTP Get"
- Verified: `<test_alert>` fired the Shuffle webhook → workflow ran (IRIS alert created, deleted after)

### Reporting crons (root crontab)
- Mon 06:45: active-response-audit.sh → reporting/output/active-response-weekly.md (verified, 0 AR events = correct)
- 1st of month 06:30: scorecard generator --live (verified: 4 endpoints, alert + flow volumes real)
- Scorecard generator fixes: live path returns aggregations only (count_total adapted); monitoring/vuln field names corrected (status, vulnerability.severity — keyword-typed already, no .keyword suffix); cardinality preferred for agent count

### Remaining
- Alert→case escalation (manual — Shuffle variables broken), credential rotation, Sysmon pilot, Greenbone credentialed scans, case template import
