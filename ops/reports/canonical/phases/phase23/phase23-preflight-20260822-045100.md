# Phase 23 Preflight - 20260822-045100

Stack root: /opt/mct-security-stack | Wazuh root: /opt/wazuh-docker/multi-node | Release: v1.1.0

## 1. Healthcheck / CI / secret / git

- Healthcheck: **0 FAIL** (20260822-045056). Local CI: PASS. Git: HEAD fd1cb3e (P22). Tags v1.0.0/v1.1.0.
- Release v1.1.0 confirmed published (API).

## 2. MAJOR: Agent 015 RECONNECTED (2026-08-22 04:22 UTC)

- Rule 501 "Agent started: 'Julians-Air->any'" at 04:22:16.
- **015 archives last 12h: 0** (was ~1.4M/day flood baseline).
- Bounded macOS telemetry flowing: location `macos` (45 alerts), sca (62), rules 5402/19008/19007/5407/533.
- Interpretation: the Mac-side repair was applied (bundle/operator). Reconnect + volume + queue
  validation now executable (Phase 23.09). 24h window accruing from 04:22.

## 3. Agent 014 (unchanged)

- Active; EID7 alerts 126/24h surviving throttle; archives 0/24h (rule-11 throttle still active).
- Tuning still required + blocked on endpoint access (Phase 23.03-06).

## 4. Agent 013

- Offline since 08-16 (power-unconfirmed). Unchanged (Phase 23.10).

## 5. Zeek v2.2 (24h)

- 316 alerts (122000: 273, 122006: 29, 122005: 13, 122001: 1) - clean window holds (~316/day).

## 6. Suricata / retention

- Suricata: ingest proven, quiet (1 event). Retention: archives-14d attached (P22 fix held).

## 7. Disk / OpenSearch watermarks - **CRITICAL WATCH**

- Root disk: **85% used** (24.2GB avail on all 3 nodes; node fs 84.7%).
- Default watermarks: low 85% / high 90% / flood_stage 95%.
- **At 84.7% the cluster is AT the low-watermark threshold** - crossing it constrains shard
  allocation; flood_stage (95%) would block writes.
- No read-only blocks currently; cluster green (266 shards, 0 unassigned).
- **Disk relief is the top capacity priority this phase.**

## 8. Swap - RESOLVED

- Swap: **707MB/8191MB (8.6%)** (was 5.2GB/64% at P22 close). vmstat si=0 - no active swap-in;
  pressure cleared with flood suppression. Stale pages returned; no action needed.

## 9. Backups / PVE222 / retention / release

- Backups fresh (snap <24h, dr-s3 <48h, config <48h).
- PVE222: **FAIL (401)** - token still missing (Phase 23.21).
- Retention: archives-14d held. Release: v1.1.0 published.

## Flags

1. **015 fixed + reconnected** - validate 24h volume/queue (top positive).
2. **Disk 85% vs low watermark 85%** - urgent relief plan (15-18).
3. 014 flood throttled; tuning blocked (endpoint access).
4. PVE222 token missing.
5. 013 power unconfirmed.

## No secrets