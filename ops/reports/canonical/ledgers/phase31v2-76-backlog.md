# Phase 31v2 Remediation Backlog (P0-P3)

Date: 2026-08-24
- P0: broader curated sensor ruleset + detection gate (packet value); endpoint markers
  (013/014) -> cert/throttles.
- P1: alert wiring (agent/sensor/backup/watermark); RAM expansion (Phase 32); disk wave
  confirmation (~08-29).
- P2: Shuffle UI controls; fresh target -> deployability PASS + full-cluster drill; NetFlow
  scope + alerts.
- P3: credential/owner closure; SLO formalization; sensor ruleset update process
  (already defined, 30).

## No secrets
