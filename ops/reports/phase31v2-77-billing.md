# Phase 31v2 Client Billing

Date: 2026-08-24
- Coverage: endpoints + packet inspection (SPAN active; detection value pending broader
  ruleset). Raw packet scanning resumed (replacing retired SO). Endpoint quality PARTIAL
  (markers pending).

## No secrets
