# Memory Tuning — 2026-08-10

## Change log

| Change | Before | After | Why |
|---|---|---|---|
| Indexer heap (3x) | -Xms1g -Xmx1g (heap used 0.70-0.82G) | -Xms768m -Xmx768m | 512m judged unsafe (heap usage would be ~80%+ constantly, GC thrash); 768m keeps headroom |
| Swapfile | none | 8G at /swapfile (fstab, swappiness=10) | OOM cushion; JVM heap should still stay in RAM |
| Backup | — | docker-compose.yml.bak 20260810-063702 + wazuh-config backup | pre-change snapshot |

## Verified

- Rolling restart one indexer at a time; cluster green after each; final green 3/3 nodes, 0 unassigned.
- Indexer RSS after tune: 1.50-1.66G each (was ~2.0G) → saved ~1.3G.
- Healthcheck PASSED (cluster green, API/indexer localhost-only, elastiflow, SO, backups).
- Free now: ~2.5G available + 8G swap.

## Capacity after tune

| Service set | RAM needed | Fits? |
|---|---|---|
| OpenCanary + Shuffle + IRIS + Velociraptor | ~2.3G with limits | YES (2.5G available; swap as cushion) |
| + MISP | +1.9G | Tight — needs feed-spike margin, risky |
| + Greenbone | +4G | NO on this host |

## Rollback

- Restore docker-compose.yml from ops/backups backup; `docker compose up -d` (restarts indexers); remove /swapfile + fstab line, `swapoff /swapfile`.
